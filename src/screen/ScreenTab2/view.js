import React from 'react';
import { View, Text, } from 'react-native';

export default self => (
    <View style={{ alignItems: 'center' }}>
        <Text style={{ fontSize: 24 }}>这是页面2</Text>
    </View>
);
